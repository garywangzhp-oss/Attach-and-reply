/**
 * One place to change, nothing else reads these values.
 */

/**
 * Where the Donate button points.
 *
 * Point this at your own page that lists the payment options - the ATN listing
 * uses the same URL in its `contributions_url` field, so there is exactly one
 * link to maintain. See README.md ("Donate") and docs/listing.md.
 */
export const DONATE_URL = "https://garywangzhp-oss.github.io/Attach-and-reply/";
